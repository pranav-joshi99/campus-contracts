/**
 * Per-section analyser output + attempt-level verdict schemas.
 *
 * Phase 1 ships this directory as a placeholder; Window C fills it in
 * Phase 2 as part of the Interview Brain v2 release + Work Area 3 report
 * rebuild. The shapes here will lock the structured numeric metrics
 * Window A persists into `attempt_reports.per_section_analysis_json` and
 * Window B reads for the spider chart + numeric panels.
 *
 * Locked schemas land in this file via the WA-3 + Interview Brain v2
 * COMMS proposals after Window C publishes the analyser output spec.
 */
export {};
